<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="snailvoyager.spring.batch.person.repository.PersonMapper">
    <select id="getAllPersons" resultType="snailvoyager.spring.batch.part3.Person">
        SELECT * FROM person
    </select>
</mapper>